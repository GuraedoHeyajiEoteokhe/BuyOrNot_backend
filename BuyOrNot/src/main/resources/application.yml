#spring.application.name=BuyOrNot
spring:
  application:
    name: ambiguous

  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://dev.macacolabs.site:1234/ambiguousdb
    username: ambiguous
    password: ambiguous

  data:
    redis:
      host: ${REDIS_HOST:dev.macacolabs.site}
      port: ${REDIS_PORT:4321}
      password: ambiguous

server:
  port: 8080

jwt:
  secret: LH9QZL8upsPBfuDY+Dkb1kT9DZIIUSuA2u4O6Lfi3mkEfeWtETpVTcR/8SMZdJWn/xNTuCQBE6rBvDXgnVmscQ==
  expiration: 1800000
  refresh-expiration: 604800000

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
